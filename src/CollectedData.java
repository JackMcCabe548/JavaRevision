interface CollectedData { // Cannot make objects of class

   public abstract void Data(); // Method must be overwritten

   static void Printer(int size, int Name) {
      for (int i = 0 ; i < size ; i++) {
         System.out.println(Name + i );
      }
   }

}
